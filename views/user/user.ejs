
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>User Profile</title>

  <!-- Font Awesome for crown icon -->

  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

  <style>
    /* Reset & Layout */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(rgba(0, 0, 0, 0.6), rgba(0, 0, 0, 0.6)),
        url('https://images.unsplash.com/photo-1740252117044-2af197eea287?q=80&w=2080&auto=format&fit=crop&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D') no-repeat center center/cover;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .profile-card {
      background-color: rgba(255, 255, 255, 0.95);
      border-radius: 16px;
      padding: 30px;
      width: 350px;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
      text-align: center;
      position: relative;
      animation: slideUp 0.6s ease-out;
    }

    @keyframes slideUp {
      0% { transform: translateY(50px); opacity: 0; }
      100% { transform: translateY(0); opacity: 1; }
    }

    .profile-picture-container {
      position: relative;
      display: inline-block;
      margin-bottom: 20px;
    }

    .profile-picture {
      width: 110px;
      height: 110px;
      border-radius: 50%;
      overflow: hidden;
      border: 4px solid #eee;
      position: relative;
    }

    .profile-picture img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }

    .status-indicator {
      position: absolute;
      bottom: 8px;
      right: 8px;
      width: 14px;
      height: 14px;
      border-radius: 50%;
      background-color: #28a745;
      border: 2px solid white;
      z-index: 5;
    }

    .crown-icon {
      position: absolute;
      top: -12px;
      left: 50%;
      transform: translateX(-50%);
      font-size: 22px;
      color: gold;
      background: white;
      padding: 4px;
      border-radius: 50%;
      box-shadow: 0 2px 6px rgba(0, 0, 0, 0.2);
      z-index: 10;
    }

    .username {
      font-size: 1.5em;
      font-weight: bold;
      color: #333;
      margin-bottom: 8px;
    }

    .user-email {
      font-size: 0.95em;
      color: #777;
      margin-bottom: 10px;
    }

    .user-location {
      font-size: 1em;
      color: #444;
      margin-bottom: 15px;
    }

    .user-stats ul {
      list-style: none;
      font-size: 0.95em;
      color: #555;
      margin-bottom: 20px;
    }

    .user-stats li {
      margin: 6px 0;
    }

    .actions {
      display: flex;
      justify-content: space-between;
    }

    .btn {
      flex: 1;
      margin: 0 5px;
      padding: 10px;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      font-weight: bold;
      transition: background-color 0.3s;
    }

    .btn-primary {
      background-color: #4CAF50;
      color: white;
    }

    .btn-secondary {
      background-color: #444;
      color: white;
    }

    .btn:hover {
      opacity: 0.85;
    }
    .btn-home {
  background-color: #00c2ff;
  color: white;
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.btn-home:hover {
  transform: scale(1.05) rotate(-2deg);
  box-shadow: 0 0 10px #04536b, 0 0 20px #00c2ff80;
}

  </style>
</head>

<body>
  <div class="profile-card">
    <div class="profile-picture-container">
      <div class="profile-picture">
        <img src="https://images.unsplash.com/photo-1740252117044-2af197eea287?q=80&w=2080&auto=format&fit=crop&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D" alt="Profile Picture">
        <div class="status-indicator"></div>

        <% if (isAdmin) { %>
          <div class="crown-icon" title="Admin">
            <i class="fas fa-crown"></i>
          </div>
        <% } %>
      </div>
    </div>

    <h2 class="username"><%= user.username %></h2>
    <p class="user-email"><%= user.email %></p>
    <p class="user-location"><%= user.country %></p>

    <div class="user-stats">
      <ul>
        <li>Age: <%= user.age %></li>
        <li>Gender: <%= user.gender %></li>
      </ul>
    </div>

    <div class="actions">
      <a href="/user/<%= user._id %>/edit" class="btn btn-primary" title="Edit Profile">
        <i class="fas fa-edit"></i> 
        <p>Edit</p>
      </a>
    
      <a href="/movie" class="btn btn-home" title="Go to Home">
        <i class="fas fa-home"></i> Home
      </a>
    
      <a href="/logout" class="btn btn-secondary" title="Logout">
        <i class="fas fa-sign-out-alt"></i> Logout
      </a>
    </div>
    
  </div>
</body>
</html>
