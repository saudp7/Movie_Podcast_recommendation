
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>CineVibes - Login / Signup</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;500;700&display=swap" rel="stylesheet" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">

  <style>
    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      padding: 0;
      background: url('/images/bg2.png') no-repeat center center/cover;
      background-size: cover;
      font-family: 'Poppins', sans-serif;
      color: #fff;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      position: relative;
    }

    .overlay {
      position: absolute;
      width: 100%;
      height: 100%;
      background: rgba(0, 0, 0, 0.75);
      z-index: 1;
    }

    .container {
      position: relative;
      z-index: 2;
      background: rgba(30, 30, 30, 0.95);
      padding: 2rem;
      border-radius: 15px;
      width: 90%;
      max-width: 700px;
      animation: fadeIn 1.5s ease-in-out;
      box-shadow: 0 0 20px rgba(255, 0, 0, 0.2);
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(-20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    h1 {
      color: #e50914;
      margin-bottom: 1rem;
      text-shadow: 0 0 10px #e50914;
      text-align: center;
    }

    .tabs {
      text-align: center;
      margin-bottom: 1.5rem;
    }

    .tabs button {
      background: transparent;
      border: 2px solid #e50914;
      padding: 0.5rem 1.2rem;
      margin: 0 0.3rem;
      border-radius: 30px;
      color: #fff;
      cursor: pointer;
      transition: 0.3s ease;
    }

    .tabs button:hover {
      background: #e50914;
      transform: scale(1.05);
    }

    .form input,
    .form select,
    .form button {
      display: block;
      width: 100%;
      margin: 0.5rem 0;
      padding: 0.6rem;
      border-radius: 30px;
      border: none;
      background: #222;
      color: #fff;
    }

    .form select {
      appearance: none;
    }

    .form button {
      background: #e50914;
      font-weight: bold;
      cursor: pointer;
      transition: 0.3s;
    }

    .form button:hover {
      background: #b00610;
      transform: scale(1.05);
    }

    .form-grid {
      display: flex;
      justify-content: space-between;
      gap: 1rem;
      margin-bottom: 1rem;
      flex-wrap: wrap;
    }

    .left-column,
    .right-column {
      width: 48%;
      display: flex;
      flex-direction: column;
    }

    @media screen and (max-width: 600px) {
      .left-column,
      .right-column {
        width: 100%;
      }
    }

    .hidden {
      display: none;
    }

    .tagline {
      margin-top: 1.5rem;
      font-size: 0.9rem;
      color: #ccc;
      text-align: center;
      animation: fadeIn 2s ease-in;
    }
  </style>
</head>

<body>
  <div class="overlay"></div>
  
  <div class="container">
    <%- include("../includes/flash.ejs") %>
    <h1>CineVibes</h1>
    <div class="tabs">
      <button onclick="showTab('login')">Login</button>
      <button onclick="showTab('signup')">Sign Up</button>
    </div>

    <!-- Login Form -->
    <div id="loginForm"  class="form">
      <form action="/login" method="POST" novalidate class="needs-validation">
        <input type="text" placeholder="Username" name="username" required />
        <input type="password" placeholder="Password" name="password" required />
        <button type="submit">Login</button>

        <!-- Inside Login Form -->
<a href="/forgot-password" style="color:#ccc; text-align:center; display:block; margin-top:10px;">Forgot Password?</a>

      </form>
    </div>


    <!-- Signup Form -->
    <div id="signupForm" novalidate class="form hidden">
      <form action="/signup" method="POST" novalidate class="needs-validation">
        <div class="form-grid">
          <div class="left-column">
           
            <input type="text" placeholder="Username" name="username" required />
            <input type="email" placeholder="Email" name="email" required />
            <input type="tel" placeholder="Phone Number" name="phoneNum" required />
          </div>
          <div class="right-column">
         
            <select name="gender" required>
              <option value="" disabled selected>Select Gender</option>
              <option>Male</option>
              <option>Female</option>
              <option>Other</option>
            </select>
            <input type="number" placeholder="Age" name="age" required />
            <input type="text" placeholder="Country" name="country" required />
          </div>
        </div>
        <input type="password" placeholder="Password" name="password" required />
        <input type="password" id="adminCodeInput" name="adminCode" placeholder="Enter secret admin code" style="display:none;">
        <button type="submit">Sign Up</button>
      </form>
    </div>

    <p class="tagline">Where Cinematic Dreams Come Alive ✨</p>
  </div>

  <script>

  document.addEventListener('keydown', function (e) {
  const input = document.getElementById("adminCodeInput");

  if (e.ctrlKey && e.shiftKey && e.key === 'K') {
    input.style.display = "block";   // Show input
  } 
  else if (e.ctrlKey && e.shiftKey && e.key === 'Z') {
    input.style.display = "none";    // Hide input again
  }
});


    function showTab(tab) {
      document.getElementById('loginForm').classList.add('hidden');
      document.getElementById('signupForm').classList.add('hidden');
      document.getElementById(tab + 'Form').classList.remove('hidden');
    }
 </script>
<script>
   (() => {
    "use strict";
    const forms = document.querySelectorAll(".needs-validation");

    Array.from(forms).forEach((form) => {
      form.addEventListener("submit", (event) => {
        if (!form.checkValidity()) {
          event.preventDefault();
          event.stopPropagation();
        }
        form.classList.add("was-validated");
      });
    });
  })();


</script>


</body>
</html>
