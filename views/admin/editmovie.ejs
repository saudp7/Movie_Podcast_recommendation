<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit Movie</title>
  <style>
    body {
      background-color: #0e0e0e;
      color: #f1f1f1;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      margin: 0;
    }

    .edit-form {
      background: #1a1a1a;
      border-radius: 16px;
      padding: 30px;
      box-shadow: 0 0 25px rgba(0, 255, 170, 0.2);
      width: 90%;
      max-width: 500px;
      animation: fadeInUp 0.8s ease-out;
    }

    .edit-form h2 {
      text-align: center;
      margin-bottom: 25px;
      color: #00ffaa;
      font-weight: bold;
      text-shadow: 0 0 8px #00ffaaaa;
    }

    .edit-form input {
      width: 100%;
      padding: 12px;
      margin: 12px 0;
      border: none;
      border-radius: 8px;
      background: #2a2a2a;
      color: #fff;
      font-size: 1em;
      transition: all 0.3s ease;
      box-shadow: 0 0 8px rgba(0, 255, 170, 0.1);
    }

    .edit-form input:focus {
      outline: none;
      background: #333;
      box-shadow: 0 0 10px #00ffaa, 0 0 20px #00ffaa55;
    }

    .edit-form button {
      width: 100%;
      padding: 14px;
      border: none;
      border-radius: 8px;
      background: linear-gradient(45deg, #00ffaa, #00c3ff);
      color: #000;
      font-weight: bold;
      font-size: 1.1em;
      cursor: pointer;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .edit-form button:hover {
      transform: translateY(-3px);
      box-shadow: 0 0 20px #00ffaa, 0 0 30px #00c3ffaa;
    }

    @keyframes fadeInUp {
      0% {
        opacity: 0;
        transform: translateY(30px);
      }
      100% {
        opacity: 1;
        transform: translateY(0);
      }
    }
  </style>
</head>
<body>
  <form action="/movies/<%= movie._id %>?_method=PUT" method="POST" class="edit-form">
    <h2>Edit Movie</h2>
    <input type="text" name="title" value="<%= movie.title %>" placeholder="Title" required />
    <input type="text" name="genre" value="<%= movie.genre %>" placeholder="Genre" required />
    <input type="text" name="year_of_release" value="<%= movie.year_of_release %>" placeholder="Year" required />
    <input type="text" name="languages" value="<%= movie.languages.join(', ') %>" placeholder="Languages (comma separated)" required />
    <input type="text" name="where_to_watch" value="<%= movie.where_to_watch %>" placeholder="Platform" required />
    <input type="text" name="imagepath" value="<%= movie.imagepath %>" placeholder="Image URL" required />
    <input type="text" name="iconpath" value="<%= movie.iconpath %>" placeholder="Platform Icon URL" required />
    <input type="text" name="imdb_rating" value="<%= movie.imdb_rating %>" placeholder="IMDb Rating" required />
    <input type="hidden" name="source" value="<%= source %>">

    <button type="submit">Update Movie</button>
  </form>
</body>
</html>
